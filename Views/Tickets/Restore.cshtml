@model BugTracker.Models.Ticket

@{
    ViewData["Title"] = "Restore";
}

<div class="row clearfix">
    <div class="col-12 col-md-6 col-lg-6 text-start">
        <div class="card">
            <div class="body">
                <h3>Are you sure you want to resore this Ticket?</h3>
                <hr />
                <div class="row g-3">
                    <div class="col-3">
                        <h5>@Model.Title</h5>
                    </div>
                    <div class="col-12">
                       <p>@Html.Raw(Model.Description)</p>
                       <hr />
                    </div>
                    
                    <form asp-action="Restore" class="ml-3">
                        <input type="hidden" asp-for="Id" />
                        <input type="submit" value="Restore" class="btn btn-success text-white" />  | 
                        <a asp-action="Index">Back to List</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


